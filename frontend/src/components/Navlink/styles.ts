import {PALETTE} from '../../constants/Palette';

export const NAVLINK_STYLES = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100%',
    padding: '1vw',
    color: PALETTE.WHITE,
};
